# recipejs-functional-cooking
# Check status
git status

# Add all files
git add .

# Commit with clear message
git commit -m "Part 3: Initial recipe rendering with static data"

git checkout -b part-3-foundation

git push origin part-3-foundation